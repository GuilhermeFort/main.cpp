import type { MysteryCase } from './case';
type Difficulty='dificil'|'especialista';
const preferredModels=['gemini-2.5-flash','gemini-2.0-flash'];
async function generate(apiKey:string,systemInstruction:string,input:string,maxOutputTokens:number,responseSchema?:object){let lastError='O Gemini não conseguiu responder agora.';for(const model of preferredModels){const response=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent`,{method:'POST',headers:{'x-goog-api-key':apiKey,'Content-Type':'application/json'},signal:AbortSignal.timeout(55000),body:JSON.stringify({systemInstruction:{parts:[{text:systemInstruction}]},contents:[{role:'user',parts:[{text:input}]}],generationConfig:{maxOutputTokens,temperature:.82,responseMimeType:'application/json',...(responseSchema?{responseSchema}:{})}})});if(!response.ok){const detail=await response.json().catch(()=>null) as any;lastError=detail?.error?.message||`Falha do Gemini (${response.status}).`;if(response.status===404||response.status===400)continue;throw new Error(lastError)}const data=await response.json() as any;const text=data.candidates?.[0]?.content?.parts?.map((p:any)=>p.text||'').join('').trim();if(!text)throw new Error('O Gemini devolveu uma resposta vazia.');try{return JSON.parse(text.replace(/^```json\s*|\s*```$/g,''))}catch{throw new Error('O Gemini não terminou de montar o caso.')}}throw new Error(lastError)}
const caseSchema={type:'OBJECT',required:['title','summary','incident','objective','difficulty','characters','clues','solution'],properties:{title:{type:'STRING'},summary:{type:'STRING'},incident:{type:'STRING'},objective:{type:'STRING'},difficulty:{type:'STRING',enum:['dificil','especialista']},characters:{type:'ARRAY',items:{type:'OBJECT',required:['id','name','role','initials','kind','publicDescription','secret','personality'],properties:{id:{type:'STRING'},name:{type:'STRING'},role:{type:'STRING'},initials:{type:'STRING'},kind:{type:'STRING',enum:['official','suspect','witness','expert']},publicDescription:{type:'STRING'},secret:{type:'STRING'},personality:{type:'STRING'}}}},clues:{type:'ARRAY',items:{type:'OBJECT',required:['key','title','description','hiddenTruth'],properties:{key:{type:'STRING'},title:{type:'STRING'},description:{type:'STRING'},hiddenTruth:{type:'STRING'}}}},solution:{type:'OBJECT',required:['culpritId','motive','method','fullExplanation'],properties:{culpritId:{type:'STRING'},motive:{type:'STRING'},method:{type:'STRING'},fullExplanation:{type:'STRING'}}}}};
const answerSchema={type:'OBJECT',required:['reply'],properties:{reply:{type:'STRING'},revealClueKey:{type:'STRING',nullable:true}}};
export async function generateMystery(apiKey:string,difficulty:Difficulty='dificil'):Promise<MysteryCase>{const seed=crypto.randomUUID(),specialist=difficulty==='especialista',suspectCount=specialist?6:5,supportCount=specialist?4:3,clueCount=specialist?12:9;const mystery=await generate(apiKey,'Você é o diretor de um jogo policial cooperativo adulto e exigente. Crie somente ficção, sem pessoas reais. O mistério precisa ser justo, internamente consistente e solucionável apenas pela combinação das pistas. Nunca escreva mensagens de espera, avisos ou texto fora do JSON.',`Crie um caso inédito em português brasileiro usando a semente ${seed}. Dificuldade: ${difficulty}. Alterne radicalmente entre homicídio, desaparecimento, sequestro, roubo impossível, sabotagem, fraude, incêndio criminoso e espionagem industrial; varie época, cidade, ambiente, vítima, método e motivo. Crie exatamente ${suspectCount} suspeitos e ${supportCount} personagens de apoio. Todo suspeito deve ter motivo plausível, oportunidade aparente, mentira verificável e segredo. O culpado deve possuir álibi forte mas desmontável. Crie exatamente ${clueCount} pistas; nenhuma isolada resolve o caso, pelo menos três precisam ser cruzadas, duas parecem incriminar inocentes, uma revela inconsistência de horário e uma depende de perícia. IDs e keys minúsculos sem espaço. culpritId deve ser suspeito.`,specialist?5200:4200,caseSchema) as MysteryCase;const suspects=mystery.characters?.filter(x=>x.kind==='suspect')||[],support=mystery.characters?.filter(x=>x.kind!=='suspect')||[];if(!mystery.title||mystery.characters?.length!==suspectCount+supportCount||suspects.length!==suspectCount||support.length!==supportCount||mystery.clues?.length!==clueCount||!suspects.some(x=>x.id===mystery.solution?.culpritId))throw new Error('O Gemini montou um caso incompleto. Tente novamente.');mystery.difficulty=difficulty;return mystery}
export async function answerMystery(apiKey:string,mystery:MysteryCase,target:string,history:string,discovered:string[],question:string){const character=mystery.characters.find(x=>x.id===target);const identity=target==='narrador'?'Central de investigação: receba pedidos de busca, perícia, registros e ações. Dê apenas resultados obtidos pela ação solicitada.':`${character?.name}, função ${character?.role}, categoria ${character?.kind}. Personalidade: ${character?.personality}. Segredo: ${character?.secret}.`;const result=await generate(apiKey,`Você conduz um interrogatório difícil em um jogo policial. Caso secreto: ${JSON.stringify(mystery)}. Papel atual: ${identity} Responda sempre como esse personagem, nunca como assistente de IA. Seja consistente. Não entregue solução sem provas. Retorne somente JSON.`,`Pistas descobertas: ${discovered.join(', ')||'nenhuma'}. Histórico: ${history}. Pergunta: ${question}. Responda em até 100 palavras. Retorne {"reply":string,"revealClueKey":string|null}.`,420,answerSchema) as any;if(!result.reply)throw new Error('Resposta incompleta do Gemini.');return result}
